.Rhistory
.RData
.Rproj.user/
.DS_Store

# Pipeline runs point at a folder anywhere on disk, but if one is ever
# nested inside this repo for local testing, don't commit the large,
# machine-specific raw data or generated output.
**/raw/
**/output/
